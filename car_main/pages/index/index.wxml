<!--start.wxml-->
<view class="container" style="background-image: url('http://198.177.123.187/car.png');  background-size:100% 100%;">
  <view class="remind-box" wx:if="{{remind}}">
    <image class="remind-img" src="/images/more/loading.gif"></image>
  </view>
  <block wx:else>
    <image class="title" src="/images/more/title8.png"></image> 
    <view class="content" >   
      <view class="hd" style="transform:rotateZ({{angle}}deg);">
      </view>
      <view class="bd">
        <view class="confirm-btn" bindtap='goToIndex' wx:if="{{regFlag==true}}">
          <text>点我，瞅个车走</text>
        </view>
        <button class="confirm-btn"  open-type="getUserInfo" bindgetuserinfo="login" wx:if="{{regFlag==false}}">
            授权登录
        </button>
        <text class="copyright">Momilean 支持</text>
      </view>
    </view>
  </block>
</view>